/**
 * חיבור ל-Supabase.
 *
 * ⚠️ **המפתח כאן ציבורי בכוונה, וזה תקין.**
 *
 * המפתח מסוג `publishable` נועד להישלח לכל דפדפן שטוען את האפליקציה —
 * הוא מופיע בקוד המקור של כל אתר שמשתמש ב-Supabase. הוא אינו סוד,
 * והוא אינו מה שמגן על הנתונים.
 *
 * מה שמגן בפועל, בשתי שכבות:
 *
 * 1. **RLS** — מדיניות במסד שמאפשרת לכל משתמש לגעת רק בשורה שלו.
 *    מי שייקח את המפתח הזה ויתחבר עם חשבון אחר לא יראה כלום.
 * 2. **הצפנה מקצה לקצה** — גם מי שיקבל גישה מלאה למסד יראה בלוב
 *    מוצפן. המפתח לפענוח נגזר מסיסמה שלא עוזבת את המכשיר.
 *
 * ⚠️ מה שאסור להגיע לכאן: `service_role`. הוא עוקף RLS לחלוטין, ולכן
 * מקומו בשרת בלבד — ולאפליקציה הזו אין שרת.
 */

export const SUPABASE_URL = 'https://mregcidikhfzmflofkzz.supabase.co';

export const SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_SAptMVO3VeyxoU6GhIpgIw_EzuBAQr_';

/** הטבלה היחידה שהאפליקציה נוגעת בה. */
export const VAULT_TABLE = 'vaults';
